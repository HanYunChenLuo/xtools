# xperf-agent 设备端二进制的交叉编译链接器（NDK clang）。
# 跨机器可移植：经 .cargo/ndk-clang.sh 按宿主机平台（macOS/Linux）探测 NDK r25 路径，
# 不再硬编码单机绝对路径；NDK 版本/API 变更时同步修改该脚本。
[target.aarch64-linux-android]
linker = ".cargo/ndk-clang.sh"
